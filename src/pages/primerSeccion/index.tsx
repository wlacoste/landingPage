import { Box } from "@mui/material";
import styles from "./index.module.scss";

const PrimerSeccion = () => {
  return (
    <Box className={styles.container}>
      <Box className={styles.seccion}>SeccionUno</Box>
    </Box>
  );
};

export default PrimerSeccion;
